# Home Assistant Satellite with Snapcast support

Create a `config.json` file and run `./install`. This was tested on an OrangePi Zero 3 with [Armbian](https://github.com/armbian/community/releases).
